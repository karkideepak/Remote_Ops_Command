Get-Item filename | Format-List *

# hash it
Get-FileHash filename

# Display Filename | Sha1
Get-ChildItem -File | ForEach-Object {
    $hash = Get-FileHash $_.FullName -Algorithm SHA1

    [PSCustomObject]@{
        Filename= $_.Name
        SHA1 = $hash.Hash
    }
}
